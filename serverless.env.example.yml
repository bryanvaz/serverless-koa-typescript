provider:
  profile: default
  region: us-east-1

dev:
  vpc:
    securityGroupId: <<SecGroupID>>
    subnetId: <<SubnetID>>
  customDomain:
    domainName: <<Domain Name>>
    # createRoute53Record: true
    endpointType: 'regional' # Use 'regional' for immediate availability 
    # certificateArn: <<Certificate ARN>>
    # certificateName: <<Certificate Name>>
    # hostedZoneId: <<Certificate Name>>
  environment:
    BASE_URL: https://sandbox.api.example.com
    # PGHOST: <<Fill PG Host>>
    # PGUSER: <<Fill PG User>>
    # PGDATABASE: <<Fill PG Database>>
    # PGPASSWORD: <<Fill PG Password>>
    # PGPORT: 9999

prod:
  environment:
    BASE_URL: https://api.example.com
    # PGHOST: <<Fill PG Host>>
    # PGUSER: <<Fill PG User>>
    # PGDATABASE: <<Fill PG Database>>
    # PGPASSWORD: <<Fill PG Password>>
    # PGPORT: 9999
